apiVersion: v1
kind: Secret
metadata:
  name: vtafarm-api-secrets
  namespace: default
type: Opaque
stringData:
  # ── Auth ─────────────────────────────────────────────────────────────────────
  JWT_SECRET: "replace-with-strong-secret"

  # ── Cloudflare ───────────────────────────────────────────────────────────────
  # API token: Zone → DNS → Edit permission on your root domain
  CLOUDFLARE_API_TOKEN: ""
  CLOUDFLARE_ZONE_ID: ""

  # ── Mediator ─────────────────────────────────────────────────────────────────
  # Shared Mediator DID assigned to all users (users cannot set their own)
  MEDIATOR_DID: ""

  # ── DID Hosting ──────────────────────────────────────────────────────────────
  # Generate with: make gen-keypair
  # Then add DID_HOSTING_DID to the hosting service ACL (role=Service, label=vtafarm)
  DID_HOSTING_CONTROL_URL: ""
  DID_HOSTING_SERVER_URL: ""
  DID_HOSTING_DID: ""
  DID_HOSTING_PRIVATE_KEY: ""
