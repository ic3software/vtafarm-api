# Committed on purpose: throwaway value, and the DB is only reachable through
# port-forward. Nothing that matters lives here — docs/shared-dev-database.md.
apiVersion: v1
kind: Secret
metadata:
  name: vtafarm-dev-postgres
  namespace: default
  labels:
    app: vtafarm-dev-postgres
type: Opaque
stringData:
  postgres-password: postgres
