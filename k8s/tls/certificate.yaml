apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ic3-dev-wildcard
  namespace: cert-manager
spec:
  secretName: ic3-dev-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "*.ic3.dev"
