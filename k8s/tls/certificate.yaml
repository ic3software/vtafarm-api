apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: firstperson-dev-wildcard
  namespace: cert-manager
spec:
  secretName: firstperson-dev-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "*.firstperson.dev"
