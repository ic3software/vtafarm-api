# Binaries
bin/
tmp/
*.exe

# Environment
.env
.env.local
.env.*.local

# Editor
.DS_Store
.idea/
.vscode/
*.swp

# Go
vendor/

# Leftover data from the removed docker-compose PostgreSQL. Kept ignored only so
# nobody commits it by accident — delete both `data/` and this rule once the
# shared dev database (docs/shared-dev-database.md) has proved itself.
data/

# Air logs
build-errors.log

# Kubernetes secrets (never commit — copy from k8s/secret.yaml.example)
k8s/secret.yaml

# Transit Vault secrets
transit-init.json
vault-init.json
