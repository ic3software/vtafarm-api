{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-postgresql
type: Opaque
stringData:
  postgres-password: {{ .Values.postgresql.password | required "postgresql.password is required" | quote }}
{{- end }}
