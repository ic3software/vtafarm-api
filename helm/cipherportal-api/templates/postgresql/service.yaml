{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-postgresql
spec:
  selector:
    app: {{ .Values.name }}-postgresql
  ports:
    - port: 5432
      targetPort: 5432
{{- end }}
